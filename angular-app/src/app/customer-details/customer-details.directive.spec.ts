import { CustomerDetailsDirective } from './customer-details.directive';

describe('CustomerDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomerDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
